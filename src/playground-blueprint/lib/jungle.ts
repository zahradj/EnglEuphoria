export type JungleAnimal = {
  name: string;
  image: string;
  letters: string[];
  colors: string[];
};

/**
 * Lesson 2 vocab is supplied by the AI unit. Bundled placeholder data
 * removed from the creator hub — structure preserved for type safety.
 */
export const JUNGLE_ANIMALS: JungleAnimal[] = [];

export const COLORS: { name: string; hex: string }[] = [];
